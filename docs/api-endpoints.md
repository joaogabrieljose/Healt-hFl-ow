# API Endpoints - HealthFlow

## 1. Visão Geral

Este documento apresenta os principais endpoints disponíveis no projeto **HealthFlow**.

O sistema utiliza um **API Gateway** como ponto central de entrada. Por isso, sempre que possível, os endpoints devem ser acedidos através da porta `8080`.

Base URL pelo Gateway:

http://localhost:8080

